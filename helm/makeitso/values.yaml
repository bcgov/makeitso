app:
  image: "ghcr.io/bcgov/makeitso"
  imageTag: "latest"
  imagePullPolicy: Always
  replicas: 1
  env: prod

  githubAppSecretName: makeitso-github-app-secret

worker:
  replicas: 1

service:
  type: ClusterIP
  port: 80
  targetPort: 8000

db:
  postgresCluster:
    storageSize: 3Gi
    postgres:
      replicaCount: 2
    pgbouncer:
      replicaCount: 2
    users:
      - name: postgres
        password:
          type: AlphaNumeric
        options: "SUPERUSER"
      - name: makeitso
        databases:
          - makeitso
        password:
          type: AlphaNumeric

route:
  host: "makeitso.apps.silver.devops.gov.bc.ca"
